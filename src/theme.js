import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FFA500",
    },
    secondary: {
      main: "#5642fc",
    },
    otherColot: {
      main: "#AAB435",
    },
  },
});

export { theme };
