import { Button, Box, Container } from "@mui/material";

function App() {
  return (
    <Container maxWidth="sm" sx={{ bgcolor: "wheat", height: "100vh" }}>
      <Button variant="contained">Hello MUI</Button>
      <Button variant="contained">Hello MUI</Button>
    </Container>
  );
}

export default App;
